## Purpose

`saleor/graphql/account/tests/mutations` (`saleor/graphql/account/tests/mutations`) groups 2 source file(s) exposing 1 top-level declaration(s).

## Public surface

**`saleor/graphql/account/tests/mutations/utils.py`**

- `generate_address_webhook_call_args` (function) — [saleor/graphql/account/tests/mutations/utils.py:10]

## How it works

The module's files, as provided to this run:

- `saleor/graphql/account/tests/mutations/__init__.py` (1 lines)
- `saleor/graphql/account/tests/mutations/utils.py` (30 lines)

## Interactions

- Imports from: _no measured outgoing edges_
- Imported by: _no measured incoming edges_

Internal dependencies named in the source:

- `.....core.utils.json_serializer.CustomJsonEncoder`
- `.....webhook.payloads.generate_meta`
- `.....webhook.payloads.generate_requestor`

## Gotchas

Documentation for this module was produced by the simulated provider. Source
fingerprint `05007b691380` — it changes whenever any file above changes, which
is what makes doc propagation testable without a paid model.
